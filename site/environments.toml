# Environment-specific baseURL configurations
[environments]
[environments.local]
baseURL = "http://localhost:1313"
description = "Local development server"

[environments.github-pages]
baseURL = "https://testuser.github.io/test-org-repo"
description = "GitHub Pages deployment"

[environments.production]
baseURL = "https://test-org-repo.org"
description = "Production domain"

[environments.railways]
baseURL = "https://test-org-repo.up.railway.app"
description = "Railways deployment"